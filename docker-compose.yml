version: '3.8'
services:
  app:
    build:
      context: ./server
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    volumes:
      - ./:/app/static:ro
      - ./bibles:/app/bibles:ro
    environment:
      - PORT=3000
      - OPENAI_API_KEY=${OPENAI_API_KEY}
